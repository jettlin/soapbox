export const videoList = (state) => state.videos?.list;
export const getLoading = (state) => state.videos?.loading;
export const getEdits = (state) => state.videos?.edits;
export const getAssetId = (state) => state.videos?.assetId;
export const getVideoId = (state) => state.videos?.videoId;
